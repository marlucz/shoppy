class Product {
  constructor({ name, quantity, unit, category }) {
    this.name = name;
    this.quantity = quantity;
    this.unit = unit;
    this.category = category;
  }
}
export default Product;
